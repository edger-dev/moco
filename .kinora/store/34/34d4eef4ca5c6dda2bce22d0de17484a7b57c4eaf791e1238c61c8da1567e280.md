# Plan — v1: ownership, and who may write

**Task:** kino://2510f2c0c82179acb1eddd256ae315f94fa2b95e6800f343a8c84b8a2e0dd2d4

The process manager's first slice is **not** a supervision feature — it is the
question every later one depends on: *whose job is this?* Lenses, restart
policies, ports and admission gates all assume an answer, and retrofitting
ownership underneath them later would touch every one of them.

Two carved specs, realized together because each is inert without the other:
ownership without an access rule changes nothing observable, and an access rule
without ownership cannot be written.

## Phase 1 — a job belongs to a workspace, and writes respect that

Prove kino://0d85bf8a00d18597ab8446e3ff9a95c7e1a25e7e0866cf33f03f4509c2f57c2f
and kino://bb38e0d68b4090f86ae94c4ca44100c4afbfd41ffce285b61f50c599dd3e4cc2.

- A **`Scope`**: `Workspace(root)` — the canonical git worktree root — or
  `System`, which is node-level and belongs to no repo. Resolution walks up for a
  `.git` entry and takes the directory holding it, in **either** form: a linked
  worktree's `.git` is a file and a main tree's is a directory, and both are
  workspaces. Outside a repo it is the canonical cwd, so there is always an
  answer and never a null.
- Every job **carries its scope**, and the scope is persisted, so ownership
  survives a daemon restart along with everything else.
- **Reads stay node-global.** `list` reports every job on the node whatever its
  scope, because "what is wedging this machine" is the question a machine-global
  supervisor exists to answer, and scoping reads would break it.
- **Writes are scoped.** A mutation carries who is asking; a job owned by another
  workspace is **refused with an error naming both workspaces**, never silently
  retargeted or silently skipped.
- The **console is the carve-out**, and it is a named one — an explicit
  `Caller::Console` rather than an absent value, so global authority is
  something a caller states rather than something it gets by omission.
- The wire carries the caller, so the distinction survives the trip.

*Tests:* a job started from a repo subdirectory is owned by the worktree root,
not the subdirectory; a linked worktree resolves to its own root and not the main
tree's; outside a repo the cwd is the workspace; a foreign workspace cannot kill,
and the refusal names both; the console can; `list` shows jobs from every
workspace; scope survives a registry restart. *Status:* **done** — `scope.rs`
(`Scope`, `Caller`), scope on the handle, the record and the wire
(`WireCaller`), the write gate on `kill`, and `scope_of` as a global read. 9 new
tests, suite green, clippy clean. Commit: `c967553`.

Two decisions worth carrying into the later phases:

- **`.git` in either form is a workspace root.** The obvious implementation
  looks only for a `.git` *directory*, which silently attributes a linked
  worktree's jobs to whatever ancestor happens to have one — and for two
  checkouts of the same repo that is precisely the confusion ownership exists
  to prevent. (Note the distinction is still needed later, and for a different
  purpose: the admission gate's `worktree = main-only` turns on directory-vs-file.)
- **Authority is claimed, never defaulted.** `Caller::Console` and
  `WireCaller::Console` are named variants rather than an absent field, so
  nothing acquires global write authority by omitting something. The same
  reasoning put the *caller's cwd* on the wire rather than a resolved
  workspace: the caller says where it is, and the engine — which owns the rule —
  says what that means.

**Not yet reachable from the agent.** The shim still builds against an older
moco revision and sends no caller, so nothing exercises the refusal end to end.
Bumping the pin and adding the caller to the shim's requests is the next step,
and it is small.

## Later phases, in dependency order

Sketched rather than specified — each gets its own plan when it is next.

1. ~~**The manifest**~~ — **done** (`e5cb8a2`). `moco-processes.styx` at the
   workspace root, `start_named`, names on jobs, hot reload on every start, and
   the gate applied to declared jobs exactly as to ad-hoc ones. 8 tests.

   Three things worth keeping:

   - **Absent and broken stayed apart**, and a duplicate name is refused rather
     than resolved last-one-wins. Both are the same instinct: a config file must
     not quietly mean something other than what it says.
   - **The gate test is the load-bearing one.** A declared job whose argv no rule
     permits still fails closed — being written in a file the agent can edit
     earns no authority. Without that test the reconciliation is just a comment.
   - **The console cannot start by name**, because a name only means something
     relative to a workspace's manifest and the console has no workspace. The
     type says so, so it is not a runtime rule to remember.

   *Not yet:* restart re-reading and re-gating the manifest, which the trust
   spec also requires — there is no `restart` verb yet, so it lands with
   lifecycle, and it must not be forgotten there.
2. ~~**Lifecycle**~~ — **done** (`434b134`). `lifetime` × `restart` ×
   `autostart`, the terminal-state interpretation, a `restart` verb, and a
   `supervise` tick. 11 tests. **The carried-forward hole is closed**: restart
   re-reads *and* re-gates.

   Three findings:

   - **A restart mints a new job id**, so a service's durable identity is
     `workspace:name` — which is what the ownership spec's fully-qualified form
     was for, arriving here rather than in the manifest phase. `declared()`
     answers "how many times has `check` restarted?" against the declaration,
     which is the question an operator actually asks. A test written against a
     single id is the wrong test, and writing one is how this surfaced.
   - **`supervise` is caller-driven, not a thread.** The engine links no
     runtime, so the daemon owns the interval — and the tick rate becomes the
     natural rate limit on a service that crashes instantly, which is a backoff
     nobody had to design.
   - **A requested stop never restarts under any policy.** `always` means
     "always when it ends on its own", not "resist being stopped".

   *Format note:* bare `true`/`false` parse as booleans, so `/bin/true` is
   `argv ("true")`. The parser refuses the mismatch rather than coercing, so it
   is a load error naming the file, not a job that runs something unexpected.
3. ~~**Lenses**~~ — **machine and terminal lenses done** (`fae221c`, `fa8a2c0`); `screen` still ahead.

   The machine lens is the half that needed no PTY, and it is the half the brief
   calls *the* feature. Measured on a job printing 400 progress lines beside a
   small JSON summary: `job_tail` returns 13941 bytes, `job_read` returns 67.
   Same question, 208x less to read.

   Three behaviours carry their own tests, all the same instinct — never let a
   read look like something it is not: the scrollback fallback **labels itself**;
   a declared-but-unwritten sidecar reads **empty** rather than silently becoming
   scrollback; and a sidecar path cannot escape the job's directory, because the
   manifest is agent-editable and a declared path is not a licence to read the
   machine.

   **Terminal lens done too** (`fa8a2c0`). `human_view @Terminal` runs the job
   under a pty so `isatty` is true, with the master pumped into the same capture
   file the log lens writes.

   It deliberately does **not** reuse `moco-tty`, and the reason generalizes:
   moco-tty forks and execs by hand, yields its own pid, and buffers in memory —
   so adopting it would have put two kinds of child in the registry and traded
   file-backed capture for an in-memory buffer, which is what durability and
   re-adoption rest on. Using `openpty` and handing the *slave* to `Command`
   keeps the child a `std::process::Child`, so every reap, kill and settle path
   is the one the log lens already uses, and moco-job still contains no `unsafe`.
   moco-tty remains the right basis for the per-job holder later — owning a
   master and a screen ring is exactly its shape — and the wrong basis for this.

   **The capture cap is done** (`a33d5da`). Compaction keeps the most recent
   bytes; the newest output is what someone is looking at.

   The truncation was the easy half. `tail` offsets were *positions in a file*,
   so compacting one silently redirects every caller's resume point onto
   different content — a poller comes back with bytes from the wrong place and
   cannot tell. Offsets are now **logical** (total bytes ever written), each job
   carries how much was discarded, and a read reports `skipped` when the caller
   asked for something already gone. The count is persisted, so a re-adopting
   daemon hands out offsets meaning what they meant before the restart.

   That gap is surfaced all the way to the agent, and the message points at the
   machine view — a job noisy enough to out-run its scrollback is precisely the
   job that should be read through a declared sidecar.

   *Still ahead:* `screen`, a VT parse over the capture stream, which now has a
   bounded stream to parse.

   Also open, and now stated: a terminal job gets a pty but **no controlling
   terminal** (no `setsid` + `TIOCSCTTY`), so redraw works while job control and
   signalling a foreground process group do not.

   **Unblocked.** The PTY-ownership question is settled in
   kino://d435f976f4bcd68e56d006a9530bb87621eac2fa06d94b8281946309dcc47a01, and
   settling it split the problem in two: lens work has no stake in PTY
   *durability*, so it is not held behind it. A `terminal` job's PTY stays
   daemon-owned and dies with it, deliberately; when that is made durable the
   mechanism is a per-job holder, tracked through the adoption path that already
   exists.

   Three facts made the decision cheaper than the brief expected: `moco-tty`
   already has a tested PTY (`openpty`, fork/exec, `setsid`, reader thread);
   `adopt` already tracks a process the daemon did not start; and a composition
   root already ships extra binaries. The load-bearing one, though, is that
   answering `screen` needs a VT parser **on our side whatever owns the master**
   — which is most of what made an external multiplexer look cheap, and is not.

4. ~~**The port authority**~~ — **done**. `port.rs` (range, reserved set, sticky
   allocation, node lock, token substitution), `port`/`port_env` on the
   manifest, allocation at `start_named` after admission, env injection, and the
   port visible on the job. 13 tests.

   Two findings, both from building it:

   - **A string that spells a boolean did not round-trip.** Styx emits a bare
     `true` for the String `"true"`, which reads back as a bool and makes the
     *whole record* undecodable — so one `/bin/true` job made the node's state
     unreadable. Fixed separately (`b92851b`) with a regression test. Phase 4's
     durability work never round-tripped such a value, which is why it survived.
   - **The spec's `$`-ban was wrong and is revised.** It refused any
     `$`-sequence in argv on the grounds that no shell is involved; but a job may
     ship a shell as `argv[0]`, and `sh -c "… $MOCO_PORT"` is then the *correct*
     way to read the injected port. Only `@MOCO_` is policed now.

   *Format note:* Styx reads a bare `@` as an enum variant, so a manifest writes
   `argv (serve --port "@MOCO_PORT")`. That is the format's rule, not the token
   contract's.
5. ~~**Admission gates** (worktree, host) and **adoption**~~ — **done**
   (`b76acd1`, `0180c87`). 15 tests.

   - The worktree gate reuses git's own directory-vs-file distinction — the
     *same fact* workspace identity uses, for a different purpose. A fixed port
     implies `main-only` as an implication an explicit value overrides, and
     `Unset` is a third variant rather than an Option because "stated vs not" is
     the distinction that matters.
   - Hosts match the **injected** node identity, never the OS hostname: reading
     `hostname` would drift from the name that actually routes.
   - Adoption is the re-adopt path parameterized, so hand-over and
     restart-recovery share one implementation instead of two that drift.
     Observe-only never respawns, which is what stops it fighting a human
     restarting that process by hand.
6. ~~**`clear`**~~ — **done** (`0180c87`). Terminal entries only, never signals,
   scope split as every other write. The durable record goes with the entry, or
   a re-adopting daemon would bring back what was just cleared.
7. ~~**`ensure`**~~ — **done** (`e22bee6`), the agent's half of the two
   triggers. Idempotent, and a refused entry is skipped **silently** because it
   runs unprompted — an explicit start still says exactly which gate refused.
8. ~~**Nothing drove `supervise`**~~ — **done**: `kudo-node` ticks it once a
   second on a blocking task, so restart policy is no longer inert.

## Resource sampling — **done** (`d54c9c4`, shim `b8e05c2`)

The question that started all of this — *what is wedging this machine?* — was
still unanswerable until now, which is worth saying plainly: every other feature
here was built on top of a motivating question none of them addressed.

Each live job is sampled on the tick `supervise` already runs. Riding that tick
rather than owning a timer means the **observed** interval is what a rate is
computed against, so there is no assumed period to drift from reality. The last
minute is kept and no more: a supervisor that grew into a metrics store would
hold data nobody queries at a cost everybody pays.

Two ways this could have quietly lied, both closed:

- **A rate needs two readings.** One `/proc` sample is a total since process
  start. Reporting it as load would show every job spiking the first time it was
  looked at — a fabricated alarm on exactly the screen someone opens when
  worried. The first sample establishes the baseline and reports no rate.
- **A job with nothing running contributes no sample, not a zero.** Zero reads
  as *idling*; absent reads as *gone*. Same distinction the machine lens draws
  with its source label, and the timeout with "the work may still be running".

Limits are **declared, reported, and never acted on** — see
kino://a26bed93558fadbe381dcd0f5e748e617f419b5f947adceb44c778f026aa2780. The
manifest fields exist now so enforcement is additive later rather than a schema
change every manifest has to follow. The `job_stats` render says outright that
nothing will act on a breach, and a test pins that phrasing: an agent told only
"over its cpu ceiling" may infer a stop is coming and kill the job first to be
tidy, which would be the refused enforcement arrived at by inference.

## Still open after this pass

- **`screen`** — a VT parse over the capture stream. The last lens, and the
  capture is bounded now, so it has a finite stream to parse.
- **`boot` autostart** has no node-level manifest to read yet; `System` scope
  exists but nothing declares into it.
- **Process groups** and the `KillMode=process` unit, both carried from v1.
- **PTY durability** via the per-job holder — decided, deferred, and the reason
  it is not blocking anything is that nothing durable is lost when a terminal
  job's daemon restarts.

Known wrinkles, none blocking: `clear` discards a declaration's port
stickiness; `adopt` writes `/` as `resolved_cwd`; the restart counter
double-counts across a restart boundary; a terminal job gets a pty but no
controlling terminal.

## Done-when (v1)

- A job's owner is a workspace resolved from a directory, persisted, and visible.
- Reads are global; writes refuse a foreign workspace with an actionable error.
- The console carve-out is explicit in the type, not implied by a default.
- Suite green, zero warnings; each realizing module cites its spec by name.
